<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.chapter_20.userscrud.UserMybatis">
    <insert id="insertData" parameterType="UserDto">
        INSERT INTO users_tbl (userid, username, userpassword, userage, useremail)
        VALUES (#{userid}, #{username}, #{userpassword}, #{userage}, #{useremail})
    </insert>
</mapper>