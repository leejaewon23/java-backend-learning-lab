<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.chapter_20.gamecrud.GameMybatis">
    <insert id="insertData" parameterType="GameDto">
        INSERT INTO game_tbl (
                               genre
                             , price
                             , img_url
                             , name
                             , grade
        ) VALUES (
                   #{genre}
                 , #{price}
                 , #{imgUrl}
                 , #{name}
                 , #{grade}
                 )
    </insert>

    <update id="updateData" parameterType="GameDto">
        UPDATE game_tbl SET
            name = #{name}
                          , price = #{price}
                          , genre = #{genre}
                          , img_url = #{imgUrl}
                          , grade = #{grade}
        WHERE id = #{id}
    </update>

    <delete id="deleteData" parameterType="Integer">
        DELETE FROM game_tbl
        WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="Integer" resultType="GameDto">
        SELECT
            id
             , name
             , genre
             , grade
             , price
             , img_url
        FROM game_tbl
        WHERE id = #{id}
    </select>

    <select id="searchList" parameterType="SearchDto" resultType="GameDto">
        SELECT
            id
             , name
             , genre
             , grade
             , price
             , img_url
        FROM game_tbl
        WHERE name LIKE CONCAT('%', #{searchName}, '%')
          AND grade = #{searchGrade}
        ORDER BY id DESC
    </select>
</mapper>