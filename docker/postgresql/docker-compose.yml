services:
  db:
    image: postgres:latest
    container_name: postgres-task
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "1234"
      POSTGRES_DB: my_post_db
    ports:
      - "5433:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql  
