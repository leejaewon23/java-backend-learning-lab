services:
  db:
    image: mariadb:latest
    container_name: mariadb-task
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: 1234
      MARIADB_DATABASE: my_maria_db
    ports:
      - "3307:3306"  
    volumes:
      - ./mariadb_data:/var/lib/mysql